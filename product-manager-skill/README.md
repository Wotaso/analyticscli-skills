# Deprecated Alias: ai-product-manager

`ai-product-manager` / `product-manager-skill` is deprecated.

Use the canonical OpenClaw Growth Engineer skill instead:

```bash
npx -y clawhub install openclaw-growth-engineer --force
```

This directory remains only so old ClawHub installs can receive a migration stub. Do not add new runtime docs or connector behavior here; update `skills/openclaw-growth-engineer/` instead.
